insecureServing:
  enabled: true
  bindPort: 8080

secureServing:
  enabled: false

db:
  debug: false
  connMaxIdletime: 0
  connMaxLifetime: 0
  maxOpenConns: 1
  driver: sqlite3
  dsn: file:test.db?cache=shared&mode=memory
  ignoreNotFound: true
