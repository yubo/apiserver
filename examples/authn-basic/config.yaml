apiserver:
  insecureServing:
    bindAddress: 0.0.0.0
    bindNework: tcp
    bindPort: 8080
    enabled: true
  secureServing:
    enabled: false
  securitySchemes:
    - name: BaseAuth
      type: base

authentication:
  anonymous: true

authorization:
  modes:
    - ABAC 
  policyFile: ./abac.json
  alwaysAllowPaths:
    - /apidocs.json
    - /swagger/*
    - /healthz

